"""Repair loop — Jeeva's core agentic organ (Phase 4).

Drives the build → run → read-traceback → patch → retry cycle inside a
:class:`~sanjeevini.sandbox.docker_sandbox.DockerSandbox`, then emits a verified
integration contract. See :mod:`sanjeevini.repair.loop`.
"""

from __future__ import annotations
